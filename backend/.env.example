APP_NAME=Akashi AI / ABSOLUTE Engine
APP_VERSION=0.2.0

# The mock provider works locally without any API keys.
AI_PROVIDER=mock

# Required only when AI_PROVIDER=gemini.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

MEMORY_FILE=data/memory.json
MEMORY_HISTORY_LIMIT=20
